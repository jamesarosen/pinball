<h1>Profiles#show</h1>
<div><%= avatar_tag(requested_profile, :size => 80) %></div>
<table>
	<tr>
		<th>Display Name</th>
		<td><%= requested_profile.display_name -%></td>
	</tr>
	<tr>
		<th>Email</th>
		<td><%= requested_profile.email -%></td>
	</tr>
	<tr>
		<th>Cell Phone</th>
		<td><%= requested_profile.cell_number -%></td>
	</tr>
	<tr>
		<th>Cell Phone Provider</th>
		<td><%= requested_profile.cell_carrier -%></td>
	</tr>
</table>
<h2>Links</h2>
<ul>
	<li><%= link_to 'Edit', edit_profile_path -%> if logged_in? and is_self?</li>
	<% if logged_in? && !is_self? -%>
	<li><%= link_to 'Follow!', follow_path(:profile_id => current_user, :follow_profile_id => requested_profile) -%> if not following?</li>
	<li><%= link_to 'Move to Tier', move_to_tier_path(:profile_id => current_user, :follow_profile_id => requested_profile) -%> if following?</li>
	<li><%= link_to 'Unfollow!', unfollow_path(:profile_id => current_user, :follow_profile_id => requested_profile) -%> if following?</li>
	<% end -%>
	<li><%= link_to 'Followers', followers_path -%></li>
	<li><%= link_to 'Following', friends_path -%></li>
	<li><%= link_to 'Tier 1 Following', following_by_tier_path(:tier => 1) -%> if is_self?</li>
	<li><%= link_to 'Tier 2 Following', following_by_tier_path(:tier => 2) -%> if is_self?</li>
	<li><%= link_to 'Tier 3 Following', following_by_tier_path(:tier => 3) -%> if is_self?</li>
	<li><%= link_to 'Friends', friends_path -%></li>
	<li><%= link_to 'Logout', logout_path -%> if logged_in?</li>
</ul>